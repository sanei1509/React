// estado inicial del argumento que le voy a mandar a mi reducer
const initialTodos = [
  {
    id: 1,
    description: "comprar fideos",
    done: false,
  },
];

const todoReducer = (state = initialTodos, action) => {
  return state;
};
